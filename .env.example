# Environment variables for weather data pipeline

# AWS Credentials (for S3)
AWS_ACCESS_KEY_ID=your_access_key
AWS_SECRET_ACCESS_KEY=your_secret_key
AWS_DEFAULT_REGION=us-east-1

# Azure Credentials
AZURE_STORAGE_CONNECTION_STRING=your_connection_string

# GCP Credentials
GOOGLE_APPLICATION_CREDENTIALS=/path/to/credentials.json

# Iceberg REST Catalog (Required for Polaris/Nessie)
# Production: Use managed Polaris service (AWS, Azure, etc.)
ICEBERG_REST_URI=https://your-polaris-instance.com
ICEBERG_WAREHOUSE=s3://your-bucket/warehouse/  # Should match storage backend

# Optional: Polaris Authentication
# ICEBERG_REST_CREDENTIAL=your_credential
# ICEBERG_CLIENT_ID=your_client_id
# ICEBERG_CLIENT_SECRET=your_client_secret

# API Configuration
WEATHER_API_TOKEN=optional_token_if_needed

# Database
DATABASE_PATH=weather_reports.db